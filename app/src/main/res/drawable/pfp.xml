<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/white" /> <!-- Fondo blanco o transparente -->
    <stroke
        android:width="4dp"
        android:color="#000000" /> <!-- Cambia el color del borde -->
</shape>
